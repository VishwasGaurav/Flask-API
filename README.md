# Flask-API
“This is my Flask API Project developed during the LinuxWorld training under the guidance of Jibbran Ali Sir. The project demonstrates how to build and deploy a backend API using the Flask framework. It handles HTTP requests such as GET and POST, processes data, and returns structured JSON responses.
